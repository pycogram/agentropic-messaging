/// Request-Reply protocol helper
pub struct RequestReply;

impl RequestReply {
    /// Create new instance
    pub fn new() -> Self {
        Self
    }
}

impl Default for RequestReply {
    fn default() -> Self {
        Self::new()
    }
}
